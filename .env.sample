# Database (for local development)
DATABASE_URL="postgresql://postgres:puka123@localhost:5432/puka?schema=public"

# Railway Postgres Database URL (for production)
# DATABASE_URL="postgresql://username:password@host:port/database"

# Authentication (no longer using NextAuth.js for this Vite app)
# These were for NextAuth.js integration which has been replaced with custom auth

# Application
NODE_ENV="development"
VITE_APP_ENV="development"
VITE_APP_VERSION="dev"
